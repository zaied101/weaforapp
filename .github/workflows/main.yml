name: test-file

on: workflow_dispatch

jobs:
  Download-Github-repo:
    runs-on : ubuntu-latest
    steps:
      - name: Create-Dir
        run: |
          mkdir ~/Test-Dir
          cd ~/Test-Dir
          git init
          git clone https://github.com/zaied54/weaforapp.git
          ls -lrt
          cd weaforapp
          ls -lrt
          
